Восстанови контекст проекта. Выполни СТРОГО по шагам — не читай лишнего:

## Шаг 1 — Последняя сессия (только последняя запись)
Прочитай `C:\Users\torew\.claude\projects\d--MyProjectCalidad-Generator-KP-Generator-KP-Actual\memory\Session-Log.md` строки 1-55.
Найди текст между первым `---` и вторым `---` — это последняя запись. Запомни:
- Какие **модули** упоминаются (числа: 05, 06, 07...)
- Что **незакоммичено**
- **Следующий шаг**

## Шаг 2 — Состояние репозитория (без чтения файлов)
Запусти только: `git status` и `git log --oneline -5`

## Шаг 3 — Модули (только упомянутые в последней записи)
Для каждого упомянутого модуля (если есть) прочитай ТОЛЬКО первые 25 строк файла:
`d:\MyProjectCalidad\Generator_KP\Generator_KP_Actual\Project_Notes\CALIDAD_BOOK\modules\{NN}_{name}.md`

Маппинг: 05→05_cockpit, 06→06_knowledge_library, 07→07_agent, 08→08_feedback_gear,
09→09_quality_control, 10→10_trainer, 11→11_analytics, 12→12_articles_seo,
13→13_erp_integration, 14→14_bitrix24, 15→15_client_portraits, 16→16_closingscript

Если модули не упомянуты — пропусти этот шаг полностью.

## Шаг 4 — Вывод (без лишнего)

---
## Контекст восстановлен — [дата]

### Где остановились
[из Session-Log: что делали, что не завершено — 2-3 строки]

### Git
- Незакоммичено: [список файлов или "чисто"]
- Последние коммиты: [5 строк из git log]

### Активные модули
[для каждого упомянутого модуля: Статус + Следующий шаг из шапки файла — 1-2 строки]

### ➡️ Следующий шаг
[конкретная задача из Session-Log]

---

ВАЖНО: Не читай MEMORY.md, last_session.md, gcp_deployment_config.md и другие файлы памяти.
Контекст проекта уже загружен автоматически через system-reminder (CLAUDE.md + MEMORY.md).
