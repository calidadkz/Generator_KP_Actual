Сохрани итоги текущей сессии. Выполни СТРОГО по шагам:

## Шаг 1 — git (только команды, без чтения файлов)
Запусти: `git log --oneline -5` и `git status`

## Шаг 2 — Синтез из контекста (не читай файлы!)
На основе ТЕКУЩЕГО ДИАЛОГА (не перечитывая файлы) определи:
- Какие **модули** CALIDAD_BOOK упоминались? (числа: 05, 06...)
- Что **сделано** в этой сессии? (1-4 пункта, конкретно)
- Что **незакоммичено**? (из git status)
- Какой **следующий шаг**? (1 конкретная задача)
- Какие **пробелы в книге** обнаружены? (если есть)

## Шаг 3 — Читай Session-Log.md и записывай новую запись В НАЧАЛО
Прочитай `C:\Users\torew\.claude\projects\d--MyProjectCalidad-Generator-KP-Generator-KP-Actual\memory\Session-Log.md`

Создай новую запись в формате (15-20 строк максимум):
```
## YYYY-MM-DD

**Сессия:** [одна фраза что делали]
**Модули:** [список номеров через запятую, или — если не было]
**Сделано:**
- [конкретный пункт]
- [конкретный пункт]
**Незакоммичено:** [файлы или "чисто"]
**Следующий шаг:** [одна конкретная задача]
[**Пробелы в книге:** ... — только если обнаружены новые]
```

Запиши файл Session-Log.md так: новая запись + `\n\n---\n\n` + весь старый контент файла.

## Шаг 4 — Обнови статус модуля в книге (только если работали с кодом модуля)
Если в сессии менялся код конкретного модуля — обнови строку `**Реализовано:**` или `**НЕ реализовано:**`
в шапке соответствующего файла `Project_Notes/CALIDAD_BOOK/modules/NN_name.md`.
Если только обсуждали или анализировали — НЕ обновляй файлы модулей.

## Шаг 5 — Вывод
Напиши одну строку: "Сохранено в Session-Log.md — [дата]"

ВАЖНО: Не обновляй last_session.md, не читай другие файлы памяти, не обновляй MEMORY.md.
